nvim/undodir
wezterm/shell-integration.sh
.claude/settings.local.json

# ya pkg-managed flavor assets -- package.toml tracks the dependency,
# the fetched files themselves don't need to be committed.
yazi/flavors/
